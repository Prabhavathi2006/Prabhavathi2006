def find_pivot_index(n, arr):
    total_sum = sum(arr)
    left_sum = 0

    for i in range(n):
        left_sum += arr[i]

        if left_sum == total_sum - left_sum + arr[i]:
            return i
n = int(input())
arr = [int(input()) for _ in range(n)]

print(find_pivot_index(n, arr))
